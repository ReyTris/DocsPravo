# Локальная разработка: только инфраструктура (Postgres + Adminer GUI).
# Приложения (apps/web, apps/worker) запускаются нативно через `pnpm dev`
# — так работает hot-reload, типы и логи нормально.
#
# Запуск:   docker compose up -d
# Стоп:     docker compose down
# Сброс БД: docker compose down -v   (удалит volume с данными)

services:
  postgres:
    image: postgres:16-alpine
    container_name: prodoki-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: prodoki
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d prodoki"]
      interval: 5s
      timeout: 5s
      retries: 5

  # GUI для базы — http://localhost:8080
  # Вход: System=PostgreSQL, Server=postgres, User=postgres, Password=postgres, Database=prodoki
  adminer:
    image: adminer:4
    container_name: prodoki-adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
